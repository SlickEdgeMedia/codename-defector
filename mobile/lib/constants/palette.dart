import 'package:flutter/material.dart';

/// Color palette for The Imposter app theme.
///
/// This defines the dark theme with gold accents used throughout the app.
class Palette {
  /// Dark background color for the app
  static const Color bg = Color(0xFF0B0C10);

  /// Panel background color (slightly lighter than bg)
  static const Color panel = Color(0xFF12131A);

  /// Primary gold accent color
  static const Color gold = Color(0xFFD4A019);

  /// Darker shade of gold for hover/pressed states
  static const Color goldDark = Color(0xFF9C7614);

  /// Primary text color (near white)
  static const Color text = Color(0xFFF5F5F5);

  /// Muted/secondary text color
  static const Color muted = Color(0xFF9EA2AF);

  /// Success/positive state color (green)
  static const Color success = Color(0xFF3CD070);

  /// Danger/negative state color (red)
  static const Color danger = Color(0xFFFF4E50);

  /// Stroke/border color
  static const Color stroke = Color(0xFF1E202A);
}
